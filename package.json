{
  "name": "mikigo.site",
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview"
  },
  "devDependencies": {
    "@rspress/core": "^2.0.0"
  },
  "dependencies": {
    "rspress-plugin-sitemap": "^1.2.1"
  }
}