name: Sync to Gitee

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code # 先检出当前代码，确保工作区存在
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 获取所有历史，而不是浅克隆

      - name: Mirror to Gitee
        uses: wearerequired/git-mirror-action@v1
        with:
          # 显式指定源仓库（当前仓库的地址）
          source-repo: https://github.com/${{ github.repository }}.git
          # 显式指定目标仓库（你的Gitee仓库地址）
          destination-repo: https://${{ secrets.GITEE_USERNAME }}:${{ secrets.GITEE_PRIVATE_TOKEN }}@gitee.com/${{ secrets.GITEE_USERNAME }}/site.git

#name: Sync to Gitee
#
#on:
#  push:
#    branches: [ main, master ]
#  schedule:
#    - cron: '0 2 * * *' # 可选：每天UTC时间2点（北京时间10点）同步一次，作为备份
#  workflow_dispatch: # 允许手动触发
#
#jobs:
#  sync:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Mirror to Gitee
#        uses: Yikun/gitee-mirror-action@master # 使用这个专门的Gitee镜像Action
#        with:
#          # 源仓库：github/用户名/仓库名
#          src: github/${{ github.repository }}
#          # 目标仓库：gitee/用户名/仓库名
#          dst: gitee/${{ secrets.GITEE_USERNAME }}/${{ github.event.repository.name }}
#          # 账户类型：个人账户填 'person'，组织账户填 'org'
#          account-type: person
#          # 使用在Secrets中配置的Gitee私人令牌
#          gitee-token: ${{ secrets.GITEE_PRIVATE_TOKEN }}
#          # 是否静态同步（通常不需要）
#          # static: false
#          # 是否强制同步（通常需要，以确保一致）
#          force: true